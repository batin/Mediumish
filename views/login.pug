extends layout

block content
  block error
  div#login-page
    form.form-signin(method="POST" action="/login")
      div.text-center.mb-4
        div#login-div

           
        
          
          
      .form-label-group
        input#inputEmail.form-control(type='email', placeholder='Email address', name="email" required='true', autofocus='true')
        label(for='inputEmail') Email address
      .form-label-group
        input#inputPassword.form-control(type='password', placeholder='Password', name= 'password' required='true')
        label(for='inputPassword') Password
      .checkbox.mb-3
        label
          input(type='checkbox', value='remember-me')
          |  Remember me
      button.btn.btn-lg.btn-primary.btn-block(type='submit') Sign in
      p.mt-5.mb-3.text-muted.text-center © 2017-2018
