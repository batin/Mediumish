nav.navbar.navbar-expand-md.navbar-dark.bg-dark.fixed-top#navbar
  a.navbar-brand.home-link(href='/') Furkan-Batin
  button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarsExampleDefault', aria-controls='navbarsExampleDefault', aria-expanded='false', aria-label='Toggle navigation')
    span.navbar-toggler-icon
  #navbarsExampleDefault.collapse.navbar-collapse
    ul.navbar-nav.mr-auto
      li.nav-item.active
        a.nav-link.home-link(href='/')
          | Home 
          span.sr-only (current)
      li.nav-item
        a.nav-link(href='/about') About
      li.nav-item
        a.nav-link(href='/contact') Contact
      if currentUser
        li.nav-item
          a.nav-link(href='/profile') Profile
      if !currentUser
        li.nav-item
          a.nav-link(href='/login') Login
      if !currentUser
        li.nav-item
          a.nav-link(href='/register') Register
      if currentUser
        li.nav-item
          a.nav-link(href='/loggedOut') Log Out
    form.form-inline.my-2.my-lg-0
      input.form-control.mr-sm-2(type='text', placeholder='Search', aria-label='Search')
      button.btn.btn-secondary.my-2.my-sm-0(type='submit') Search
    
